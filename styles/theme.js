import { createTheme } from "@material-ui/core";

const theme = createTheme({
    palette: {
        primary: {
            main: '#F26157',
        },
        secondary: {
            main: '#39A2AE'
        }
    }
})

export default theme
